name: boltzina_env
channels:
  - defaults
  - conda-forge
  - bioconda
dependencies:
  - bioconda::maxit
  - python=3.10
  - pip
  - pip:
    - -e /home/aoxu/projects/VLS-Benchmark-Dataset-boltzina/external/boltzina
    - rdkit
    - gemmi
